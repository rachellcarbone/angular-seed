### Redirect to new domain ###
# RewriteEngine on
# RewriteRule ^(.*)$ http://www.rachellcarbone.com/$1 [R=301,L]

### Redirect to www ###
# RewriteEngine on
# RewriteCond %{HTTP_HOST} ^rachellcarbone.com [NC]
# RewriteRule ^(.*)$ http://www.rachellcarbone.com/$1 [R=301,L]

### Redirect iphones to moble site ###
# RewriteEngine on
# RewriteCond %{HTTP_USER_AGENT} iPhone
# RewriteRule .* http://moble.rachellcarbone.com/ [R]

### Redirect all requests to the index.html ###
# RewriteEngine on
## Don't rewrite files or directories
# RewriteCond %{REQUEST_FILENAME} -f [OR]
# RewriteCond %{REQUEST_FILENAME} -d
# RewriteRule ^ - [L]
## Rewrite everything else to index.html to allow html5 state links
# RewriteRule ^ index.html [L]