'use strict';

/* 
 * Page Layout: Member
 * 
 * Used to display the layout for and control elements 
 * that are consistent across the member (authenticated)
 * area of the app.
 */

angular.module('layout.member', [])
        .controller('MemberLayoutCtrl', ['$scope', function ($scope) {
        
    }]);