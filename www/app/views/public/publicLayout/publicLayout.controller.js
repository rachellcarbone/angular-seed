'use strict';

/* 
 * Page Layout: Public
 * 
 * Used to display the layout for and control elements 
 * that are consistent across the public (un authenticated)
 * area of the app.
 */

angular.module('app.public.layout', [])
        .controller('PublicLayoutCtrl', ['$scope', function ($scope) {
        
    }]);