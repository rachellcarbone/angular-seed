'use strict';

/* 
 * Public Landing Page
 * 
 * The main not authenticated, public, landing page (index/home/default).
 */

angular.module('app.public.landing', [])
    .controller('PublicLandingCtrl', ['$scope', function($scope) {
        
    }]);