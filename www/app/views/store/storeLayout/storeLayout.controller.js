'use strict';

/* 
 * Page Layout: Store
 * 
 * Used to display the layout for and control elements 
 * that are consistent across the public (un authenticated)
 * area of the app.
 */

angular.module('app.store.layout', [])
        .controller('StoreLayoutCtrl', ['$scope', function ($scope) {
        
    }]);