'use strict';

/* 
 * Admin Roles Page
 * 
 * Controller for the admin roles page where user roles can be viewed and mofified.
 */

angular.module('app.admin.roles', [])
    .controller('AdminRolesCtrl', ['$scope', function($scope) {

        
    }]);