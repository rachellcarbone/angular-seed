'use strict';

/* 
 * Page Layout: Admin
 * 
 * Used to display the layout for and control elements 
 * that are consistent across the administrative
 * area of the app.
 */

angular.module('layout.admin', [])
        .controller('AdminLayoutCtrl', ['$scope', function ($scope) {
        
    }]);