'use strict';

/* 
 * Page Layout: Member
 * 
 * Used to display the layout for and control elements 
 * that are consistent across the auth (login, logout, etc)
 * area of the app.
 */

angular.module('app.auth.layout', [])
        .controller('AuthLayoutCtrl', ['$scope', function ($scope) {
        
    }]);