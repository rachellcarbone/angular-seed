'use strict';

/* 
 * Page Layout: Error
 * 
 * Used to display the layout for and control elements 
 * that are consistent across app error pages.
 */

angular.module('app.error.layout', [])
        .controller('ErrorLayoutCtrl', ['$scope', function ($scope) {
        
    }]);